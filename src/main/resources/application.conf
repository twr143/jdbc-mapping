slick {
    profile = "slick.jdbc.PostgresProfile$"
    db {
        url = "jdbc:postgresql://localhost:5432/jdbc-mapping?currentSchema=schema1"
        user = "jdbc_mapper"
        password = "123"
        driver = "org.postgresql.Driver"
        numThreads = 5
        maxConnections = 5
        minConnections = 1
    }
}
q-ctx{
    jdbcUrl="jdbc:postgresql://localhost:5432/jdbc-mapping?currentSchema=schema1"
    dataSource.user="jdbc_mapper"
    dataSource.password="123"
    dataSource.numThreads = 5
    dataSource.minConnections = 1
    dataSource.maxConnections = 5
    connectionTimeout=30000
}